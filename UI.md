# Todo 앱 UI 초안

이 문서는 **화면을 어떻게 보이게 할지**에 대한 참고용입니다.  
앱 기능 코드가 아니며, Phase 1~5를 만들 때 이 방향을 따릅니다.

## 한 줄 방향

조용하고 읽기 쉬운 **단일 화면 Todo**.  
대시보드처럼 복잡한 카드·통계·배지는 쓰지 않습니다.

## 화면 구성 (한 화면)

위에서 아래로 이 순서만 둡니다.

1. **앱 이름** — `Todo` (작지 않게, 화면의 주된 제목)
2. **짧은 안내** — 한 문장 (예: `오늘 할 일을 적어 보세요.`)
3. **입력 영역** — 텍스트 입력 + 추가 버튼
4. **목록 영역** — 할 일 항목들

첫 화면에는 통계, 필터 탭, 로그인, 사이드바를 넣지 않습니다.

## 레이아웃

- 가운데 정렬된 **하나의 세로 흐름**
- 최대 너비 약 `480px` (모바일에 가깝게, 읽기 편함)
- 좌우 여백을 넉넉히 두어 답답하지 않게
- 카드로 모든 것을 감싸지 않음. 입력과 목록이 구분만 되면 충분

```text
┌─────────────────────────────┐
│  Todo                       │
│  오늘 할 일을 적어 보세요.   │
│                             │
│  [ 입력창 ............ ] [추가] │
│                             │
│  · 장보기                   │
│  · 운동하기                 │
│  · 메일 확인                │
└─────────────────────────────┘
```

## 색과 분위기 (CSS 변수로 쓸 값)

어두운 테마, 보라 그라데이션, 과장된 그림자는 쓰지 않습니다.

| 이름 | 용도 | 값 |
|------|------|-----|
| `--bg` | 배경 | `#EEF2F4` |
| `--surface` | 입력/목록이 앉는 밝은 면 | `#FBFCFD` |
| `--text` | 본문 | `#1C2430` |
| `--muted` | 안내 문구 | `#5C6B7A` |
| `--accent` | 추가 버튼 | `#0F766E` |
| `--accent-text` | 버튼 글자 | `#FFFFFF` |
| `--line` | 얇은 구분선 | `#D5DEE5` |
| `--danger` | (Phase 3) 삭제 | `#B42318` |

배경은 단색만 쓰지 말고, 아주 약한 **위에서 아래로의 밝은 그라데이션**  
(`#EEF2F4` → `#E4EBEF`) 정도만 둡니다. 장식용 이미지는 필요 없습니다.

## 글꼴

시스템 기본 글꼴(Arial, Inter 등)만 쓰지 말고, 읽기 좋은 조합을 씁니다.

- 제목: `Fraunces` 또는 `Literata` (조금 또렷한 감각)
- 본문·입력: `Source Sans 3` 또는 `IBM Plex Sans`

웹폰트는 Google Fonts 등으로 연결해도 되고, Phase 1에서는 연결만 해 두고 꾸밈은 최소로 둡니다.

## 요소별 규칙

### 앱 이름
- 페이지에서 가장 먼저 눈에 띄는 텍스트
- 다른 헤드라인보다 작아지지 않게

### 입력창
- 한 줄 입력
- 테두리 색은 `--line`, 포커스 시 `--accent`
- 플레이스홀더 예: `할 일을 입력하세요`

### 추가 버튼
- 라벨: `추가`
- 배경 `--accent`, 글자 `--accent-text`
- 둥근 모서리는 적당히 (`8px` 전후). 알약형(pill) 남용 금지

### 목록 항목
- 한 줄에 하나의 할 일
- 항목 사이는 얇은 선(`--line`) 또는 작은 간격만
- Phase 1: 텍스트만 보여도 됨
- Phase 2: 왼쪽에 체크 공간
- Phase 3: 오른쪽에 삭제 공간

### 빈 목록
- 항목이 없을 때: `아직 할 일이 없습니다.` (`--muted`)

### 빈 입력 막기
- 추가되지 않으면 됨
- 필요하면 입력창 아래 짧은은 안내 한 줄 (요란한 팝업 금지)

## Phase별 UI 범위

| Phase | UI에서 하는 일 |
|-------|----------------|
| 1 | 이름, 안내, 입력, 추가, 목록 텍스트 |
| 2 | 체크 표시(완료 시 글자에 취소선 + 흐린 색) |
| 3 | 삭제 버튼(`--danger` 계열, 작은 텍스트 버튼) |
| 4 | 화면 변화 없음 (저장은 뒤에서) |
| 5 | 전체 간격·대비·깨짐만 점검 |

## 하지 말 것

- 보라/인디고 그라데이션 테마
- 다크모드 기본 적용
- 네온 글로우, 다중 그림자
- 통계 카드, 배지, 플로팅 스티커
- 첫 화면에 필터·검색·설정 패널
- 이모지로 기능을 대신하기

## 움직임 (나중에 최소만)

학습용이므로 애니메이션은 필수 아님.  
넣는다면 아래 정도만:

1. 항목이 추가될 때 짧게 fade-in
2. 버튼 hover 시 배경이 조금 진해짐
3. 완료 체크 시 취소선이 자연스럽게 생기게

## 확인 체크 (디자인)

- [ ] 앱 이름이 주인공처럼 보이는가
- [ ] 한 화면에 할 일이 “입력 → 목록”으로 바로 읽히는가
- [ ] 색이 과하지 않고 글자가 읽히는가
- [ ] Phase에 없는 UI(삭제, 필터 등)를 미리 넣지 않았는가

---

이 문서를 바꾸지 않는 한, 구현은 위 규칙을 따릅니다.
