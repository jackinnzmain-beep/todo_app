# Todo 앱 PLAN

학습 목적의 초보자용 Todo 앱 계획 문서입니다.  
이 파일은 코드가 아니라 **프로젝트 지도와 진행 기록**입니다.

## 프로젝트 목표

브라우저에서 돌아가는 Todo 앱을 만들고,  
**추가 → 완료 체크 → 삭제 → 새로고침해도 유지**까지 학습한다.

## 범위

### 필수 (이번 프로젝트에서 한다)

- Todo 추가
- 완료 체크
- Todo 삭제
- LocalStorage로 브라우저 저장

### 제외 (이번 프로젝트에서 하지 않는다)

- 검색·필터
- 수정(텍스트 다시 고치기)
- 우선순위·마감일
- 회원가입·로그인
- 알림·공유·여러 기기 동기화
- 캘린더·복잡한 분류

> 예전에 말했던 “선택 기능 Phase”는 이번 계획에서 **제외**한다.

## 현재 상태

| 항목 | 내용 |
|------|------|
| 현재 Phase | 없음 (준비 단계) |
| 상태 | 진행 전 |
| 다음 할 일 | Rules 확인 후 Phase 1 설명·승인 |

## 기술·저장 결정

| 항목 | 결정 |
|------|------|
| 형태 | 브라우저 웹 앱 |
| 구성 | HTML + CSS + JavaScript (프레임워크 없음) |
| 데이터 저장 | Phase 4에서 LocalStorage |
| 작업물 보관 | Git으로 성공한 상태를 저장 (세이브 포인트) |
| Git | 사용함. 검증 통과 후 Commit |
| Rules | `.cursor/rules/todo-app-collaboration.mdc` |
| UI 참고 | `UI.md` |
| 제외 목록 | `.cursorignore` |
| Git 제외 | `.gitignore` |

## Phase 목록

### Phase 1 — Todo 추가

| 항목 | 내용 |
|------|------|
| 목표 | 입력한 일을 목록에 보이게 한다 |
| 상태 | 진행 전 |
| 완료 조건 | 입력 후 추가하면 목록에 나타난다. 빈 내용은 추가되지 않는다(또는 경고). |
| 제약 | 완료 체크, 삭제, 저장 기능은 만들지 않는다 |
| 완료 기록 | (아직 없음) |

### Phase 2 — 완료 체크

| 항목 | 내용 |
|------|------|
| 목표 | 끝난 일을 표시한다 |
| 상태 | 진행 전 |
| 완료 조건 | 체크하면 해당 Todo만 완료로 보인다. 다시 누르면 미완료로 돌아온다. 다른 Todo는 그대로다. |
| 제약 | 삭제, 저장, 요청하지 않은 기능은 만들지 않는다 |
| 완료 기록 | (아직 없음) |

### Phase 3 — Todo 삭제

| 항목 | 내용 |
|------|------|
| 목표 | 필요 없는 일을 지운다 |
| 상태 | 진행 전 |
| 완료 조건 | 삭제하면 그 Todo만 사라진다. 추가·체크 기능은 그대로 작동한다. |
| 제약 | 저장, 검색, 대규모 구조 변경은 하지 않는다 |
| 완료 기록 | (아직 없음) |

### Phase 4 — LocalStorage 저장

| 항목 | 내용 |
|------|------|
| 목표 | 새로고침해도 목록이 남게 한다 |
| 상태 | 진행 전 |
| 완료 조건 | 추가·체크·삭제 후 새로고침해도 결과가 유지된다. 브라우저를 닫았다 열어도 목록이 남는다. |
| 제약 | 새 기능 추가나 큰 구조 변경은 하지 않는다. 저장만 붙인다. |
| 완료 기록 | (아직 없음) |

### Phase 5 — 최종 QA

| 항목 | 내용 |
|------|------|
| 목표 | 전체 기능을 다시 점검한다 |
| 상태 | 진행 전 |
| 완료 조건 | 추가 → 체크 → 삭제 → 새로고침 흐름이 문제없다. 화면이 크게 깨지지 않는다. 자동 검사와 직접 사용 확인을 통과한다. |
| 제약 | 새 기능을 추가하지 않는다. 발견한 문제만 고친다. |
| 완료 기록 | (아직 없음) |

## 작업 원칙

1. 한 번에 **하나의 Phase**만 진행한다.
2. 구현 전에 목표·완료 조건·건드리면 안 되는 범위를 설명한다.
3. 사람이 승인한 뒤에만 코드를 수정한다.
4. AI가 “완료”라고 해도, 브라우저에서 직접 확인하기 전에는 완료로 보지 않는다.
5. 검증을 통과한 뒤 Git에 Commit으로 저장하고, 이 문서의 상태·완료 기록을 갱신한다.

## 진행 순서

```text
Phase 1 추가
  → Phase 2 체크
  → Phase 3 삭제
  → Phase 4 저장
  → Phase 5 최종 QA
```

## 메모

- 기술·저장 방식은 위 “기술·저장 결정”을 따른다.
- 화면 방향은 `UI.md`를 따른다.
- Git 저장 시점: Phase 검증 통과 후. 이름 예) `Phase 1: Todo 추가 기능`
- 폴더/파일 구조의 구체안은 Phase 1 시작 직전에 설명한다.
- 이 문서는 Phase가 끝날 때마다 갱신한다.
